// Config file with specific settings for production environment
{
  // Parameters pertaining to the Catenis application
  "application": {
    "cryptoNetwork": "bitcoin",   // Available values: 'bitcoin' (for Bitcoin's production blockchain), 'testnet' (for Bitcoin's test blockchain)
    "shutdownTimeout": 30000   // Time, in miliseconds, that the application should wait after it receives a terminate request before it actually terminates
  },
  // Parameters pertaining to logging mechanism
  "logging": {
    "email": {
      "toAddresses": "andre@blockchainofthings.com, jason@blockchainofthings.com, claudio@hiades.com.br"
    }
  },
  // Parameters pertaining to BitcoinCore module
  "bitcoinCore": {
    "serverHost": "10.0.0.4" // Bitcoin server host name/IP address
  }
}
